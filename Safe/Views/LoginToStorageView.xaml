<UserControl x:Class="EdlinSoftware.Safe.Views.LoginToStorageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
            <TextBlock Text="{DynamicResource PasswordWithColon}" Style="{StaticResource TextBoxLabel}" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}"/>
            <Button Content="{DynamicResource Login}" Command="{Binding LoginCommand}" />
            <Button Content="{DynamicResource Cancel}" Command="{Binding CancelCommand}" />
    </StackPanel>
</UserControl>
